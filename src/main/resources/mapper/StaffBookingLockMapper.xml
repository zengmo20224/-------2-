<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petcare.booking.mapper.StaffBookingLockMapper">

    <!--
        Uses a composite key based id: staffId * 10000 + dayOfYear to ensure uniqueness
        across different dates while remaining deterministic for tests.
        MERGE INTO works in both H2 and MySQL 8.0+.
    -->
    <update id="upsertStaffBookingLock" parameterType="map">
        MERGE INTO staff_booking_lock (id, staff_id, booking_date, create_time, update_time)
        KEY (staff_id, booking_date)
        VALUES (#{id}, #{staffId}, #{bookingDate}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </update>

    <select id="selectStaffBookingLockForUpdate" resultType="com.petcare.booking.entity.StaffBookingLock">
        SELECT id, staff_id, booking_date, create_time, update_time
        FROM staff_booking_lock
        WHERE staff_id = #{staffId} AND booking_date = #{bookingDate}
        FOR UPDATE
    </select>

</mapper>
