# Production build — API base URL must be configured at deploy time.
# For local preview against a running backend, override with:
#   VITE_API_BASE_URL=http://localhost:8080 npm run build:h5
VITE_API_BASE_URL=

# Local mp-weixin real-device preview. Replace with the HTTPS production API
# before release, or override these values in the shell/build environment.
VITE_MP_API_BASE_URL=http://10.100.153.71:8080
VITE_MP_API_BASE_FALLBACK_URLS=http://10.110.37.66:8080,http://192.168.137.1:8080
